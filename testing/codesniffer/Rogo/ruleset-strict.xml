<?xml version="1.0"?>
<ruleset name="ExamSys">
    <description>The ExamSys coding standard</description>
    <!-- Skip 3rd party code-->
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/plugins/texteditor/plugin_tinymce_texteditor/mee/mee/tools/include/*</exclude-pattern>
    <exclude-pattern>*/plugins/questions/enhancedcalc/rserve/*</exclude-pattern>
    <exclude-pattern>*/LTI/classes/*</exclude-pattern>
    <exclude-pattern>*/LTI/ims-lti/lti_util.php</exclude-pattern>
    <exclude-pattern>*/qti/include/simpleimage.php</exclude-pattern>
    <exclude-pattern>*/cosign/*</exclude-pattern>
    <!-- Skip config file and it's backups created during upgrades -->
    <exclude-pattern>config/config.*.php</exclude-pattern>
    <!-- Only look at php files -->
    <arg name="extensions" value="php,inc" />
    <!-- Rules.
    We follow PSR-12 with some caveats due to old code base-->
    <rule ref="PSR12">
        <!-- Namespaces and classes DO NOT follow PSR-0 -->
        <exclude name="PSR1.Classes.ClassDeclaration"/>
    </rule>
    <!-- As we support PHP 7.2 as a minimum constant visibility should error -->
    <rule ref="PSR12.Properties.ConstantVisibility">
        <type>error</type>
    </rule>
    <!-- Only use double quotes if necessary-->
    <rule ref="Squiz.Strings.DoubleQuoteUsage"/>
    <!-- Old code has a lot of double quotes containing vars so just warn for now -->
    <rule ref="Squiz.Strings.DoubleQuoteUsage.ContainsVar">
        <type>warning</type>
    </rule>
    <!-- Perl style comments not allowed -->
    <rule ref="PEAR.Commenting.InlineComment"/>
    <!-- Warning if php doc missing from class -->
    <rule ref="PEAR.Commenting.ClassComment.Missing"/>
    <!-- Warning if php doc missing from function -->
    <rule ref="PEAR.Commenting.FunctionComment.Missing"/>
    <!-- Warning if php doc missing from file -->
    <rule ref="PEAR.Commenting.FileComment.Missing"/>
    <!-- Warning if commented out code detected - set by default at 35% valid code-->
    <rule ref="Squiz.PHP.CommentedOutCode"/>
    <!-- Warning if php file contains HTML-->
    <rule ref="Generic.Files.InlineHTML">
        <type>warning</type>
    </rule>
    <!-- ExamSys specific coding standards warning for now as lots to fix -->
    <rule ref="Rogo.Classes.AutoloadFilename">
        <!-- Exclude specific testing directories that are not really autoloading locations. -->
        <exclude-pattern>*/testing/unittest/tests/classes/*</exclude-pattern>
    </rule>
</ruleset>
